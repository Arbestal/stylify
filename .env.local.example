ANTHROPIC_API_KEY=sk-ant-...

# The variables below are provisioned automatically by Vercel (Neon Postgres +
# Vercel Blob) when this project is linked with `vercel link`. Run
# `vercel env pull .env.local` instead of filling these in by hand.
DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
